# ECE 289 Project - Neural Style Transfer

Implementation of *A Neural Algorithm of Artistic Style* (Gatys et al.) using PyTorch.

### Folder layout
See `nst/`, `scripts/`, and `assets/` for modular code.

### QUick start
```bash
python scripts/verify_setup.py
